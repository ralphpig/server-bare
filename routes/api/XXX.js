// Dependencies
var router = require("express").Router();
var util = require("../../util/util");
var auth = require("../../util/auth.js");

// Models
var db = require("../../util/mysql");

// Routes
//router.route("/").all(auth.XXX);

module.exports = router;
